

class Comentario {
  constructor(id, texto, usuarioId, treinoId, exercicioId) {
    this.id = id
    this.texto = texto
    this.usuarioId = usuarioId
    this.treinoId = treinoId
    this.exercicioId = exercicioId
  }
}

export default Comentario;